{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local file open/save permissions for the main editor window. Scope is intentionally limited to user document/desktop/download folders. If a user reports they want to save elsewhere, add the specific subtree here rather than widening to $HOME/**.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:menu:default",
    "core:window:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    {
      "identifier": "fs:scope",
      "allow": [
        "$DOCUMENT/**",
        "$DESKTOP/**",
        "$DOWNLOAD/**"
      ]
    }
  ]
}
